virtualmachines:
  hosts:
    devuan:
      ansible_host: devuan
      ansible_user: fwaguette
      ansible_ssh_private_key_file: ~/.ssh/id_anxiety
      ansible_become_password: "{{ devuan_pass }}"

xeonmachine:
  hosts:
    birdcage:
      ansible_host: birdcage
      ansible_user: toucan
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519_bc_client
      ansible_become_password: "{{ birdcage_pass }}"

rasppi:
  hosts:
    applecider:
      ansible_host: applecider
      ansible_user: rocky
      ansible_ssh_private_key_file: ~/.ssh/id_anxiety
      ansible_become_password: "{{ applecider_pass }}"
